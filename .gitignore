# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Build output — Vercel runs `npm run build` and serves its own output, so a
# committed dist/ is never deployed and only goes stale.
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ingest PDF cache
scripts/.cache

# env & local configs
.env
*.env
**/.env

# server-only local stuff
src/server/.env
src/functions/.env
.env
*.env
**/.env
src/server/.env
src/functions/.env
